# Validation of the priority continuation

22 September 2026. This continuation changes only its new research, evidence,
documentation and companion-draft directories, plus byte-preservation
attributes. It does not alter the original manuscript, `src/`, `app/` or main
test suite. The previously recorded main-suite result remains historical
(1,672 passed, 2 slow tests deselected); it was not rebranded as a new run.

## Executed checks

- Isolated research suite: **161 passed**, three upstream Qiskit deprecation
  warnings, **226.47 seconds**. Command includes the previous 109-test research
  scope plus the new priority directory, with `PYTHONNOUSERSITE=1` and
  `.context/controlled_closeout_repro/Scripts/python.exe`. Additional final
  wrapper/decoding checks then passed **20 tests**; these overlap the earlier
  suite and are not added to claim 181 distinct tests. The committed-checkout
  replay is recorded in the release receipt.
- Entire compiled original-arithmetic wave sources: C4 f40, H8 f40 and phase
  f64 execute emitted gates, compare with exact integer targets and restore all
  inputs/workspace. Entire range-specialized C4/H8 sources pass the same checks.
- Composed range/wave financial+selector and financial+phase passes execute
  both inverses. Selector checks straddle the exact marking boundary; phase
  checks use each tight estimator's actual first target and normalizer.
- Independent estimator review rechecks all 149 stage certificates, exact
  binomial tails, 288 direct matrix-power laws and 186 Grover-sign cases.
- Compiler review exhausts 253,952 intermediate values on a separate small
  graph and checks all **25** saved schedules' copies, dependencies, counts and workspace.
- Independent capacity review reconciles 360 work-capacity coordinates and
  all six combined rows, including restricted factory and noise-fit premises.
- All 95 preserved rotation strings pass independent exact-target interval
  verification. Their new multiplicities reconcile with the combined ledger.
- A seven-page companion PDF was generated, text-extracted and visually
  inspected; editable Markdown and HTML accompany it. Author approval is pending.

These are tests, deterministic proof calculations and basis execution checks.
They are not a hardware experiment, full pricing-QAE execution, statistical
price confirmation or a guarantee that independent mathematical review cannot
find another issue.

## Repository scope and limits

Targeted Ruff passes for the complete new research directory.
The recorded broader repository lint debt (4,675 historical findings) remains;
no full-repository lint pass is claimed. The existing environment lacks mypy,
and no successful typecheck is claimed. Byte-hashed historical evidence is
preserved rather than reformatted to hide those limitations.

The continuation manifest excludes itself and later release-validation receipts
to avoid recursive hashes. It includes the actual research sources, reports,
compiled leaves, result ledgers and draft. Every frozen file must verify, as
must the preserved historical artifact/snapshot manifests. The release receipt
records the exact computational commit and final checks; it is not an approval
of unmet scientific gates.
