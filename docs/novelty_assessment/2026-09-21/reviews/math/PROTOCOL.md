# Independent bounded mathematical checks

Date: 2026-09-21. Author: independent AI mathematical review agent. This is
an assessment check of existing development evidence, not a new pricing
acquisition, held-out case, or confirmation experiment.

Question: do independent exact-rational calculations reproduce the signed
residual inequalities and selected schedules, and do selected production-grid
boundary/central nodes contradict the pointwise arithmetic certificates?

Fixed inputs: all 18 records in `signed_residual_arithmetic_v1`; selected
`D1_00` and `D2_00`; the existing 148-row primary archive; unchanged D1/D2
models, q10, cutoff 4, width 48 / fraction 24, and the existing $1/95%
ideal-logical contract. Read archives without modifying them. Record hashes
of every input and of this protocol and check script.

Checks fixed before execution:

1. Reconstruct rational monomial conversion, quantization, normalization,
   Horner, scale, residual, shift, discounted error and AE inequalities in all
   18 records. Obtain an independent rational pi enclosure from Machin's
   identity and alternating arctangent sums. Do not import certificate producers.
2. Verify the 17-trial majority failure bound exactly using a binomial upper
   bound with single-trial failure probability 1/5. Verify the recorded power
   of two M is the smallest admissible value under the archived deterministic
   allowance, using the independent pi enclosure.
3. Quantify unused schedule margin and the additional price allowance
   `L_upper * 2**-53` sufficient for correctly rounded binary64 AE labels.
   This does not certify unbounded-error libm sine or implement a decoder.
4. At selected D1/D2 q10 nodes with each coordinate index in
   `{0, 1, 511, 512, 1022, 1023}`, reconstruct integer arithmetic independently
   and compare with an independently enclosed exact stored-model payoff and
   degree-four control. This is 36 D1 and 1,296 D2 nodes, fixed in advance.
   Verify parent per-spot error, sum-scaled residual error, threshold range,
   and decoded pointwise residual. These selected-node diagnostics cannot
   prove a uniform claim over the production grid.
5. Record identical quantized coefficient lists across nominal primary
   degrees, and exact residual-sign / restricted-normalization counterexamples.

Pass: all archived upper bounds dominate the reconstructed expressions, all
shift and schedule checks pass, and every fixed node satisfies the stated
certificate. Any failure is retained and reviewed; there is no retuning.

Limits: one process, at most 2,000 selected nodes, no gate emission, no
statevector, no external services, expected under 120 seconds. The standard
library Decimal exp is used at 110 digits and enlarged by one representable
neighbor; all remaining arithmetic is exact rational. Input logs are enclosed
before exponentiation. The parent universal exponential proof and finite-model
offset expectation are reviewed analytically and remain inherited assumptions
for the residual-inequality check; this script is not a new implementation of
every historical certificate.

Output: exclusively `independent_checks.json` beside this file; refuse to
overwrite it. This protocol and the script may be committed as assessment
deliverables. Frozen sources and evidence remain untouched.
