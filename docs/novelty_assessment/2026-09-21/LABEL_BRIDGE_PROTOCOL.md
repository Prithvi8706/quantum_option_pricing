# Bounded exact-label decoding check

Date: 2026-09-21. Written before running the new adapter or enumerating its menu.

Question: does a certified conversion of the exact canonical AE label
`sin²(pi*y/M)` to the residual decoder's binary64 input consume enough dollar
allowance to change any of the 18 archived signed-residual schedules?

Keep every archived model, certificate, cost and schedule unchanged. Add the
opt-in `research.assessment_20260921.label_bridge` adapter outside the frozen
producer directory. It uses the existing directed trigonometric enclosure,
then bounds the discrepancy of the actual returned float. For an odd sample,
sorted interval endpoints enclose the median. Charge `L_upper * eta`, where
eta bounds conversion for every possible label, in addition to the archived
deterministic budget (which already charges affine constants and two float
operations). The allowance is a classical numerical term, not physical error.

Menu: all labels 0 through M-1 for each distinct M in the 18 archived rows;
at most 4,096 labels per M and 20,000 total evaluations, five minutes CPU.
Recheck each schedule with exact rational arithmetic and an independent
Machin-series rational upper bound on pi. Success means the original M still
fits $1 and remains minimal dyadically. Failure means record which schedule
needs revision; do not alter its frozen evidence or conceal the failure.

Check exact special angles, folded-label symmetry, nontrivial median ordering,
ambient Decimal precision independence, and invalid inputs. Write only to a
new assessment receipt with exclusive creation. Record source/input hashes.
This is deterministic interface validation, not a pricing acquisition, fresh
confirmation, physical execution, or a new algorithmic result.
