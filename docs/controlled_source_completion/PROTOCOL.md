# Controlled source completion

22 September2026. User explicitly authorized continuing this direction through
implementation despite the earlier failed feasibility gate. Do not search for
another financial workload or advantage mechanism. Preserve prior evidence.

Deliverables in order:

1. Executable hierarchical coherent source for the SAME controlled compound
   Asian-basket estimator: preparation, path arithmetic, geometric-put control,
   continuation policy, clipping/decision, signed output and inverse cleanup.
   Every arithmetic leaf must have an emitted reversible implementation, not an
   assumed oracle. A hierarchical gate file is acceptable; call it hierarchical,
   not a flattened full-device circuit. Tables must be finite, explicit and paid.
2. Explicit Kothari--O'Donnell known-second-moment estimator using the complex
   phase primitive and an adaptive interval contraction. State all constants,
   controlled-phase/global-phase conventions and confidence repetitions.
3. Complete logical cost ledger and a remaining-work checklist. Then execute
   the two highest implementation priorities on that checklist: (P1) independent
   evaluator/cleanliness/spectral validation, (P2) complete logical resource and
   precision reconciliation, including every preparation, inverse, control and
   synthesized/unsynthesized rotation obligation. Neither item may be called a
   financial numerical certificate or a hardware experiment.

The financial accuracy/advantage criterion remains penny absolute error,
99% per price,10x latency over the strongest eligible classical implementation,
with broad confirmation only after feasibility. Quantum implementation may
use a finite distribution and finite arithmetic, but it must not silently reuse
continuous-model moment/bias certificates as if they applied to that distribution.

Use direct reversible Box--Muller generation from midpoint binary uniforms as
an implementation candidate within this same estimator. Preparation is explicit
Hadamards; logarithm, square root and trigonometric evaluation are charged.
Clipping/range guards define a digital target and require a separate bridge to
the original GBM contract. Keep the previous geometric/parity controls and fitted
policy available to the classical comparator.

Record development diagnostics on all four prior models with fixed seed root
2026092601. Use representative C4 and H8 complete compilations and shared leaf
libraries for all models if compilation costs warrant. First digital precision
profiles are24 fractional/16 random bits and40 fractional/32 random bits, with
32 integer bits, spot guard [2^-16,4096], and exact modular arithmetic semantics.
No target precision is certified merely because a paired pilot agrees.

Published theory is read from the already archived full manuscript; additional
documentation lookup, if required for implementation, is not a new direction
search. No manuscript advantage claim, held-out case, deployment or external
message is authorized by this implementation work.
