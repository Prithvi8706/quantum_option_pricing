# Study W2: frozen bounded application validation

The W1 candidate remains standby: no production replacement, confirmation
promotion, remote publication or hardware submissions. Same arithmetic Asian
basket and GBM. This protocol precedes new acquisitions, not external registration.

1. Integrate product-normal loading, the original/reflection signal, archived
   residual QSP, classical degree-four offset, Hadamard readout and canonical
   amplitude-estimation (AE). A small d2/q1/degree16 circuit is executed by
   statevector, including AE with three phase-estimation qubits; simulated
   measurement draws are clearly labeled. No production-size simulation claim.
2. Use q10/cutoff4 and archived degrees16,32,64,128 on fresh acquisitions:
   development D1=(assets1,dates2,sigma.2,K95,rho.3),
   D2=(2,2,.25,105,.4); reserved evaluation E1=(1,3,.35,110,.6),
   E2=(2,2,.4,90,.7). Evaluate all, without tuning on E1/E2. These are reserved
   development evaluations, NOT independent statistical confirmation; no claim
   their parameter values have never appeared in any historical sweep.
3. For both encodings certify ideal logical deterministic dollar error,
   including representation, payoff, residual coefficient bridge, archived
   QSP phase response, signal/preparation, stored radius and projector phases,
   offset and final scalar rounding. Keep hardware/native decomposition error
   unknown. Do not silently set an unknown execution error to zero.
4. Target absolute dollar tolerance1 at confidence95%. Canonical AE uses a
   worst-case amplitude error pi/M+pi^2/M^2, success>=8/pi^2, independently
   repeated odd median with Hoeffding failure bound. Count A and its inverse,
   controlled operations, reflections, QFT and classical preprocessing. Gate
   projections are labeled composition estimates, not QPU runtime measurements.
   Cap planned total A-equivalent calls at10 million; fail closed otherwise.
5. Same continuous target: compare antithetic MC+geometric control, RQMC+control,
   conditional RQMC+control at 16 independent replicates, powers10 and12, and
   separately fit 1024 pilot paths. Include setup/pilot and evaluation time.
   Student-t replicate intervals are approximate diagnostics, not rigorous
   finite-sample certificates. Do not compare their wall time to simulated
   quantum wall time as a hardware advantage test.
6. Frozen decision: among logically feasible candidates minimize projected
   composition CX; otherwise refuse. Production/confirmation remains refused
   without a physical implementation budget. Retain losses, non-deliveries,
   baseline overhead, and setup costs. Check tiny finite exact targets apart
   from continuous-target diagnostics.
7. Targeted primary-source prior-art review; report whether a journal-level
   novelty/advantage claim is actually supported. Finish with tests, separate
   environment replay and logged go/no-go, not endless favorable tuning.

Completion of this bounded work package may be a no-go. It does not mean
physical execution, independent peer review, or the blocked historical
confirmation campaign has been completed.
