# Current completion checklist

22 September 2026. This supersedes the status in the preserved
[earlier checklist](../controlled_source_completion/CHECKLIST.md). The user's
instruction to continue authorized the follow-up despite the earlier stopping
recommendation. It did not change the advantage threshold.

| Priority | Work performed | Scientific completion condition |
|---|---|---|
| P1 | Existing source/estimator validation independently reviewed; alias-copy depth and relocation issues fixed | Completed as implementation checks, not a full financial proof |
| P2 | Logical costs reconciled; new and historical artifacts audited | Completed as an audit; feasibility screen fails |
| P3 | Exact truncated multiplier emitted, tested, integrated and recosted | **Not achieved:** 1.91x T improvement does not meet the complete 10x latency budget |
| P4 | q32 finite-law/clipping/control bridge proved; low-precision uniform-error claim falsified | **Partial:** f40 arithmetic, finite-policy regret, surrogate accuracy and tight-moment transfer remain open |
| P5 | Actual rotation sequences certified; 32 conditional physical scenarios include exposure, routing, factories and retry failures | **Partial:** no validated layout/decoder or complete financial execution budget; unknown terms explicit |
| P6 | Independent technical and paper-claim review; evidence package prepared | **Advantage confirmation not eligible:** P3-P5 have not passed; no held-out success or publication acceptance asserted |

What remains for an **advantage** claim, in dependency order:

1. A compiled source and explicit estimator that pass the cost gate, then a
   renewed comparison with the strongest applicable classical pricing methods.
   The present implementation does not pass even the favorable source-only screen.
2. The complete continuous-price certificate: f40 joint arithmetic/range error,
   implemented-policy regret, surrogate accuracy and a valid digital moment.
3. A supported physical execution model or mapping with all full-price costs,
   outputs and failure probabilities charged. Conditional sensitivity tables
   are available; actual execution and layout validation are not.
4. Only after those gates: freeze an independent parameter set and classical
   tuning protocol, then run the 24-case robustness confirmation at the original
   thresholds. Development cases cannot be reused as independent confirmation.

What remains for a **journal submission** is a different checklist: select a
defensible benchmark/methodology contribution, establish its novelty against
existing resource-estimation and controlled-pricing literature, write a separate
claim around the validated contract, and choose a suitable venue. Those steps
do not fulfill the original advantage objective. No manuscript rewrite or
submission is included in this merge.
