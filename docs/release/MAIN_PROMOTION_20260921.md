# Verified main promotion — 21 September 2026

The user separately authorized independent review and promotion to main after
the earlier assessment had deliberately left main unchanged. A new separate
AI agent reviewed completed batches 1–4 and found no actionable integration
blocker. See the [independent review](MAIN_PROMOTION_REVIEW_20260921.md).
This is AI integration review, not human scientific approval.

## Completed promotion

Remote main advanced by ordinary fast-forward from
`da3080d19a8f746cab31bbee157a5d591c6b56fb` to the reviewed dev payload
`0146372fc3ab85e787906ea9181fae982d7156e2`. A subsequent `git ls-remote`
confirmed that exact SHA. Local main was synchronized by a non-forced fetch
from its older `103ddfd1` pointer to the same reviewed payload. Dev already
contained and had published all ten promoted commits. No commit was rebased,
squashed, rewritten or cherry-picked; acquisition hashes and authorship remain.

GitHub's main protection endpoint reported "Branch not protected"; repository
rulesets and open PR lists were empty. No protection was bypassed. Only main
and dev remain as local and remote development branches. The reviewer did not
change Git refs or author the implementation it reviewed.

The promoted scope includes stronger arithmetic and signed residual source,
tests, frozen evidence, the numerical label adapter, preserved historical
artifacts and the candid scientific assessment. Completed batches were
reviewed together and promoted as one complete snapshot, so the numerical
interface correction accompanies the evidence it qualifies.

## Verification performed for this promotion

- Fresh targeted tests: **19 passed** (15 adapter tests and four existing
  decoder tests); targeted Ruff passed.
- Fresh prepromotion consistency check: **300 local links** and **143
  receipt/source/input hash bindings** passed. Frozen implementation remained
  unchanged from its original source commits.
- Independent reviewer read all **13,783 preserved artifacts**, totaling
  **254,488,741 bytes**, and matched their manifest SHA-256 values.
- Independent resource reconstruction passed **148 primary rows, 18 residual
  rows, 332 ledgers and two reflection references**. This is deterministic
  replay, not fresh pricing observations or all-circuit gate reconstruction.
- Historical **1,645/315** test receipts remain valid evidence for unchanged
  code, with overlapping populations; they were not rerun or added together.

The assessment's old delivery verifier and receipt deliberately assert the
prepromotion main pointers. They remain historical artifacts and are not a
postpromotion branch-health command. This record supersedes their current
branch-status interpretation without changing their original evidence.

## Closeout and remaining work

This factual promotion record, the independent review and current navigation
pointers are added in a subsequent closeout commit on dev, pushed to dev first,
then promoted to main by ordinary fast-forward. Final remote equality is
checked after that push and reported in the task closeout; the payload SHA
above records the already-completed scientific integration.

The [publication decision](../novelty_assessment/2026-09-21/PUBLICATION_DECISION.md)
remains conditional go for a narrow comparator case-study draft. The common
compiler-policy comparison and manuscript/publication package (future batches
5–6) have not been performed. Physical error guarantees, quantum advantage,
fresh confirmation and qualified human significance assessment remain open.
Integration into main does not close any of these research gates.
