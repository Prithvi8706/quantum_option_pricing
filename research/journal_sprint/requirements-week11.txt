# Python 3.9, Windows research/replay environment; separate from web-app dev deps.
# Pins reproduce the numerical stack; clean source-checkout testing uses the
# already installed environment. A fresh installation is not yet validated.
-r ../../requirements.txt
-r requirements-legacy-circuit.txt
mpmath==1.3.0
psutil==7.2.2
